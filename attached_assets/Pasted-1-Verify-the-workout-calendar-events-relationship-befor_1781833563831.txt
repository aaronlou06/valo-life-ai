1. Verify the workout ↔ calendar_events relationship before Round 0 builds anything. The Agent decided reschedule_workout.execute mutates a calendar_events row with type = "workout". The entire feature's correctness rests on that assumption. But your Workout Co-Pilot has its own relational schema (sessions, templates, programs) — so the question is whether a scheduled workout is a calendar_events row, or whether calendar_events is a mirror of a separate workout_sessions/scheduled-workout table. If it's a mirror and execute only touches the calendar row, you'll desync the calendar from the actual workout the user sees in the Co-Pilot. This is exactly the "pull the actual files, don't trust the Agent's read" case — before building, check the real schema and confirm which table is canonical. If both exist, execute/undo must update the source of truth (and the mirror), and the snapshot must capture both.
2. Round 1: generate proposals in the nightly job, not synchronously inside GET /api/home-briefing. The Agent left this as "whichever is cleaner." It's not a toss-up — a Claude call in the briefing request path adds latency to every Home load. Put generation in nightlyInsights.ts (it already listed it as relevant) and have the briefing route only read pending proposals. Make that the instruction, not an option.
3. Round 3: the execute endpoint must re-validate freshness server-side before mutating. Round 2 checks staleness at render, but a card can sit on screen while the underlying data changes, then Accept fires execute against stale parameters. Before mutating, execute should re-confirm the source event is still at its expected slot and the target slot is still open — and reject (return a "this changed" state) if not. Otherwise the guardrail "nothing mutates wrongly" has a hole at the one moment it matters.
Two minors worth a line in the prompt: Round 4's test should also assert the Thursday move is excluded within the 7 days, not only that it frees up after 8 (the plan only tests the expiry side). And confirm generateActionProposals lands somewhere coherent — buildVapiContext.ts is about assembling Vapi context, so the engine function shouldn't be buried there; next to generatePreCallIntelligence is the right home.
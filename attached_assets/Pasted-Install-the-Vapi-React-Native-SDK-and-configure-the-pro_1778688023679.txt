Install the Vapi React Native SDK and configure the project for native voice calls using my specific Vapi assistant. Do the following: 1. Install these packages exactly: - @vapi-ai/react-native - @daily-co/react-native-daily-js - @react-native-async-storage/async-storage - react-native-background-timer - react-native-get-random-values - @daily-co/react-native-webrtc at exactly version 118.0.3-daily.4 - @config-plugins/react-native-webrtc - @daily-co/config-plugin-rn-daily-js - expo-build-properties 2. Update app.json with the following changes: - Add to the plugins array: "@config-plugins/react-native-webrtc" and "@daily-co/config-plugin-rn-daily-js" - Add a third plugin entry: ["expo-build-properties", { "android": { "minSdkVersion": 24 }, "ios": { "deploymentTarget": "13.4" } }] - Add to the ios config inside infoPlist: { "NSMicrophoneUsageDescription": "Valo needs microphone access to conduct your evening debrief.", "UIBackgroundModes": ["voip"] } - Set bitcode to false in the ios config 3. Create or update android/gradle.properties and set newArchEnabled=false 4. Create a file at lib/vapi.ts with this exact content: import Vapi from '@vapi-ai/react-native'; const vapi = new Vapi(process.env.EXPO_PUBLIC_VAPI_KEY!); export default vapi; 5. Create a file at hooks/useVapiDebrief.ts that does the following: Import vapi from lib/vapi.ts and useState, useEffect from react. Define a type CallState = 'idle' | 'loading' | 'active' | 'ending' Define a type TranscriptEntry = { role: 'assistant' | 'user'; text: string } Export a custom hook useVapiDebrief that accepts userId: string as a parameter and returns: callState, transcript (TranscriptEntry array), startCall function, endCall function, isMuted (boolean), toggleMute function. Inside the hook: - Set up useEffect to register these Vapi event listeners on mount and clean them up on unmount: - vapi.on('call-start', () => set callState to 'active') - vapi.on('call-end', () => set callState to 'ending', then process the transcript) - vapi.on('speech-start', () => no UI change needed) - vapi.on('message', (message) => if message.type is 'transcript' and message.transcriptType is 'final', append { role: message.role, text: message.transcript } to the transcript array) - vapi.on('error', (error) => log the error and set callState back to 'idle') - The startCall function should: a. Set callState to 'loading' b. Fetch the context payload from GET /api/vapi/context/:userId c. Call vapi.start() with this exact configuration: { assistantId: "1f6a73ec-abef-4181-ac53-31b6a037a613", assistantOverrides: { variableValues: contextPayload } } d. If any error occurs set callState back to 'idle' and show an Alert - The endCall function should call vapi.stop() - The toggleMute function should call vapi.setMuted(!isMuted) and toggle the isMuted state - The transcript processing on call-end should: a. Wait 1 second for final transcript events to arrive b. POST to /api/debrief/process with { userId, transcript } c. Set callState to 'idle' 6. Create a .env file (if it doesn't exist) and add these placeholder lines: EXPO_PUBLIC_VAPI_KEY=your_vapi_public_key_here EXPO_PUBLIC_VAPI_ASSISTANT_ID=1f6a73ec-abef-4181-ac53-31b6a037a613 7. Add EXPO_PUBLIC_VAPI_ASSISTANT_ID to Replit Secrets with the value: 1f6a73ec-abef-4181-ac53-31b6a037a613 Note: The assistant is already configured in the Vapi dashboard with ElevenLabs Elliot voice and Claude Haiku 4.5 as the model. The vapi.start() call does NOT need to pass model or voice configuration — those are set on the assistant itself. The only override passed at call time is variableValues containing the user's real-time context data (user_name, sleep_score, hrv_today, hrv_avg, and all other wearable and lifestyle variables).
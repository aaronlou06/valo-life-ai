Verification events: typed FKs, not a polymorphic id. It proposes a generic itemType + itemId. Don't — you already carry FK debt (the serial/text mismatch), and a polymorphic FK-less column adds orphan risk and manual cascade handling. Use nullable habitId + routineId with a check that exactly one is set: still one table, still one write path, but referential integrity holds. And since you can reseed the test account trivially, I'd make this a cleanly-named new verification_events table rather than overloading habit_completions — a foundational table should be named for what it is. (That second part's your call; the typed-FK part I'd hold firm on.)
Defer the Verify/Anchor role field. The spine already excludes calendar events by querying only habits and routines, and work/school are already type="work". The only consumer of an explicit anchor field is the planner — a later build whose requirements you don't know yet. The distinction stays honored by table structure; add the field when the planner actually needs it instead of modeling it blind now. (Your work/school-as-anchor intent is intact — this just avoids speculative schema.)
Reserve the enum values, defer the mode column. Add assumed to the provenance and status enums now — cheap, and it saves an enum migration later. But don't add an unused mode column to both the habit and routine tables for a flip you haven't designed; it'll likely land in the wrong place. Model it with the flip build.
Keep the habit heuristic — and make the catch-up grid handle flexible habits as a weekly roll-up. Don't add day-of-week scheduling to habits now; that's a habit-model overhaul. The heuristic (daily habits get a daily occurrence; <7/week habits are weekly-target with no per-day miss) is correct and avoids false misses. The edge to bake in: a flexible habit doesn't fit a per-day grid, so the catch-up surface has to show it as a weekly cell ("1 of 3 runs this week"), not empty day-cells. (Day-of-week habit scheduling is your eventual habit/routine unification — later.)
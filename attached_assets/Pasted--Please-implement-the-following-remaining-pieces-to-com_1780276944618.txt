"Please implement the following remaining pieces to complete the Valo app overhaul:
Backend APIs — Ensure all core routes exist and are JWT-protected: /api/checkin, /api/today/cards, /api/goals, /api/calendar, /api/health, /api/insights, /api/meal, and /api/integrations/*. Create any missing DB migrations for: daily_summaries, weekly_rollups, insights_patterns, google_tokens, nutrition_logs, and reminders tables.
Today Cards — Create a computeTodayCards(userId, date) service that runs a nightly aggregation job and returns exactly 1–3 prioritized cards via POST /api/today/cards. Each card must have: id, type, title, subtitle, data, and primaryAction with label, actionType, and payload.
VAPI — Finalize the VAPI system prompt to enforce one question at a time, soft-close when core areas are covered, reference the user's onboarding motivation, and output exactly one line on close: SUGGESTED_ACTION: "<label>" | "<actionType>" | <payload_json>. Ensure /api/vapi/context returns the required context block and /api/vapi/debrief stores mood, energy, wins, stressors, and intentions from the transcript.
Meal Planner — Build POST /api/meal/plan accepting dietType, caloriesTarget, budget, and allergies, and returning a structured AI-generated meal plan using Claude with strict JSON output including daily meals, macro estimates, and a shopping list. Build frontend screens for MealPlannerScreen, MealSwapModal, and GroceryListScreen. Add Instacart as an opt-in connection in the Profile tab — when connected, allow the user to send their grocery list directly to their Instacart cart. If Instacart is unavailable, fall back to CSV export and copy-paste. Ask explicit user consent before storing or sending shopping data to Instacart.
Insights Engine — Create a nightly correlation job that populates an insights_patterns table, feeds the top 2 patterns plus the user's top goal to Claude, and stores the result in insights_cache. Claude must return strict JSON: {insight, explanation, suggested_action: {label, actionType, payload}}. The Insights tab should show one prioritized insight card with a 'Why this?' option that shows the raw source datapoints. Show a proper empty state when no real data exists — no mock or seeded data.
Wearable & Health Integrations — Add Garmin and Whoop as connect/disconnect options in the Profile integrations hub alongside Google Calendar, Apple Health, and Google Fit. Show last sync time per integration. All connections live exclusively in Profile — no connection management anywhere else in the app.
Feature Flags — Create a feature_flags table and a server-side helper that can toggle features by user or by percentage rollout. Gate all major new features behind flags so they can be turned off safely.
Telemetry — Instrument the following events and wire to PostHog or Amplitude: today.screen_view, today.primary_cta_click, today.time_to_first_action, checkin.start, checkin.complete, insight_shown, meal_plan_generated. These are required for measuring cognitive load and primary CTA conversion.
Account Controls — Ensure the Profile tab includes: account data export, account deletion with confirmation dialog (server-side soft-delete before purge), and password reset. Voice transcripts and images must be optional and deletable — do not store raw audio unless necessary.
Staging & QA — Set up a persistent staging environment, create a TestFlight distribution checklist, add an in-app feedback micro-survey that stores responses in a DB table, and confirm the telemetry dashboard is connected. The rollout plan is: internal sanity → 3 friends week 1 → 10 friends week 2 → larger alpha week 3 with monitoring.
Security reminders — Confirm TOKEN_ENCRYPTION_KEY is used to encrypt all refresh tokens, all secrets are stored in Replit secrets, and third-party integration scopes are clearly listed to the user before they connect."